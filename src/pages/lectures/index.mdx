---
title: Lectures
description: "What is covered, week by week, and which topics each lecture draws on."
---

import LecturesGrid from "../../components/LecturesGrid.astro";
import { deckLecture } from "../../lib/deck";

export const deck = await deckLecture();

# Lectures

One a week, and none of them long. Each ends with a single claim put plainly
enough to be argued with, and the Nap Lab later that week is where you get to
argue with it.{deck ? ` Week ${deck.data.week} comes with slides.` : ""}

<LecturesGrid />
